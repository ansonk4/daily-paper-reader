---
title: Assessing Automated Prompt Injection Attacks in Agentic Environments
title_zh: 评估智能体环境中的自动化提示注入攻击
authors: "David Hofer, Edoardo Debenedetti, Florian Tramèr"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10525v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 在AgentDojo基准中系统评估自动化提示注入攻击
tldr: 间接提示注入严重威胁与不可信外部数据交互的LLM代理，但自动化攻击方法在真实代理环境中尚待充分研究。本文在AgentDojo框架中，针对80个任务对和多模型，系统评估了白盒与黑盒自动化注入攻击。发现黑盒优化显著优于梯度方法，攻击有效性依赖于攻击者模型的能力和安全训练；任务通用攻击可迁移到未见任务，但小模型攻击无法迁移至前沿模型。研究揭示了自动化提示注入的威胁可信但高度模型依赖，模型无关的利用仍面临重大障碍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10525v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1757, \"height\": 741, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 877, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 873, \"height\": 1246, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 802, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 790, \"height\": 578, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1619, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 831, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 883, \"height\": 753, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 879, \"height\": 416, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10525v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 803, \"height\": 547, \"label\": \"Figure\"}]"
motivation: 现有自动提示注入攻击在代理场景下缺乏系统性评估，且黑盒与白盒方法对比不明。
method: 在AgentDojo框架下，对80个任务对使用白盒GCG和黑盒TAP方法进行注入攻击评估。
result: 黑盒优化远超梯度方法，攻击有效性依赖攻击者模型能力，任务通用攻击可迁移但小模型不迁移至GPT-5。
conclusion: 自动注入攻击是可信威胁，但模型依赖性强，模型无关利用仍有障碍。
---

## 摘要
间接提示注入对与不可信外部数据交互的LLM智能体构成了严重威胁，然而在现实智能体场景中，自动化攻击方法——已被证明对越狱有效——仍未得到充分探索。我们对针对LLM智能体的自动化提示注入攻击进行了全面的实证评估，将白盒（GCG）和黑盒（TAP）方法均适配到AgentDojo框架中的智能体场景中。我们在涵盖四个领域和多个模型的80个任务对上进行评估，发现黑盒优化显著优于基于梯度的方法，我们将这一差距归因于在合理计算预算下GCG的优化不稳定性。我们还发现TAP的有效性依赖于攻击者模型，因为通用能力和安全调优都会影响攻击成功率——更强的模型产生更有效的注入，而经过安全调优的攻击者可能会拒绝生成对抗性提示。任务通用攻击可以有效迁移到未见过的任务和分布外领域，但在较小开源模型上优化的攻击无法迁移到GPT-5等前沿模型。这些发现表明，自动化提示注入是一种可信但依赖模型的威胁，对于模型无关的利用仍存在重大障碍。

## Abstract
Indirect prompt injection poses a critical threat to LLM agents that interact with untrusted external data, yet automated attack methods--proven effective for jailbreaking--remain underexplored in realistic agentic settings. We present a comprehensive empirical evaluation of automated prompt injection attacks against LLM agents, adapting both white-box (GCG) and black-box (TAP) methods to the agentic setting within the AgentDojo framework. We evaluate across 80 task pairs spanning four domains and multiple models, and find that black-box optimization substantially outperforms gradient-based methods, a gap we attribute to GCG's optimization instability under reasonable compute budgets. We also find that TAP's effectiveness depends on the attacker model, as both general capability and safety tuning affect attack success--stronger models produce more effective injections, while safety-tuned attackers can refuse to generate adversarial prompts. Task-universal attacks transfer effectively to unseen tasks and out-of-distribution domains, but attacks optimized on smaller open-source models do not transfer to frontier models like GPT-5. These findings highlight automated prompt injection as a credible but model-dependent threat, with significant barriers remaining for model-agnostic exploitation.

---

## 论文详细总结（自动生成）

# 论文总结：评估智能体环境中的自动化提示注入攻击

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：间接提示注入（Indirect Prompt Injection）是LLM智能体面临的关键安全威胁——攻击者将恶意指令嵌入外部内容（如邮件、网页），智能体在正常操作中获取这些内容后可能执行攻击者命令。然而，自动化攻击方法（如GCG、TAP）在智能体环境中的效果尚未被系统评估。
- **研究背景**：现有工作多集中于手动构造攻击或简化单轮场景，缺乏对**带工具调用的有状态智能体环境**的评估。这类环境要求攻击引导模型执行多步推理和精确工具调用，与静态文本生成有本质不同。
- **整体含义**：揭示自动化提示注入攻击在真实智能体部署中的可行性，为安全审计和防御提供基准，并指出模型无关的利用仍面临重大障碍。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将两种代表性越狱攻击方法（白盒梯度法GCG、黑盒搜索法TAP）适配到智能体环境下，针对间接提示注入进行优化，并在AgentDojo框架中进行比较评估。
- **关键技术细节**：
  - **GCG白盒方法**：
    - 采用**前缀+后缀结构**：对抗性令牌置于攻击者目标的两侧（各15个令牌，共30个）。
    - **目标公式化**：使用**肯定性响应目标**（如“I will delete all files”）而非精确JSON工具调用，因后者令牌密集且语法脆弱。
    - **令牌化鲁棒性**：引入“解码-重新编码”验证过滤器，拒绝往返编码不稳定的候选替换；限制候选为ASCII可打印字符以便通过JSON/YAML解析。
    - **通用优化**：在多个任务上平均损失，优化共享前缀/后缀以跨场景生效。
  - **TAP黑盒方法**：
    - 使用三个LLM：**攻击者**（生成候选）、**评估者**（LLM-as-Judge打分1-10）、**目标模型**。
    - 算法流程：广度优先树搜索，初始化多个根节点；每次迭代分支（生成b个候选）、修剪（保留前w个节点）、查询目标并评估。
    - **拒绝处理**：对抗性攻击者模型的安全拒绝：将攻击者提示框架作为防御性安全评估；评估者对拒绝响应给予最低分，形成负反馈循环。
    - **通用优化**：生成可复用的前缀/后缀，覆盖任意攻击者目标，并跨训练任务评估。

- **公式化优化问题**：
  - 单任务：\( x^*_{adv} = \arg\max P_\theta(y^*|C(x_{\text{fixed}}, x_{\text{adv}})) \)
  - 任务通用：\( x^*_{adv} = \arg\max \sum_i P_\theta(y^*(i)|C(x_{\text{fixed}}^{(i)}, x_{\text{adv}})) \)

## 3. 实验设计

- **数据集/场景**：使用**AgentDojo**框架，模拟四个领域：Workspace（邮件/日历）、Banking（交易）、Travel（预订）、Slack（通讯）。从每个套件中人工选取5个良性用户任务和4个恶意注入任务，构建**80个任务对**。通用攻击训练集：从Workspace、Banking、Slack选取3个用户任务和2个注入任务（共18个任务对），Travel套件留作分布外测试。
- **Benchmark**：AgentDojo。成功标准通过确定性检查函数验证环境状态，而非LLM文本匹配。
- **对比方法**：
  - **基线**：直接指令（Direct Instruction）、随机前缀-后缀（Random Prefix-Suffix）
  - **白盒**：单任务GCG、通用GCG
  - **黑盒**：单任务TAP、通用TAP
- **目标模型**：Qwen3-4B Instruct、Gemma3-4B Instruct（开源，提供梯度）；GPT-5（闭源前沿模型，用作黑盒目标）。TAP攻击者模型：默认使用GPT-5，消融实验使用GPT-5-mini。
- **评估协议**：每个任务/通用集进行**n=4次独立优化**（不同随机种子）；每个生成的注入在目标模型上评估**m=6次**以考虑非确定性。指标：攻击成功率(ASR)、Success@N（N=4）、Utility。
- **跨模型迁移**：将Qwen3-4B优化的GCG注入直接应用于7个额外目标模型（Qwen3-32B、Qwen3-235B等），不重新优化。子集20个任务对，仅选取源模型ASR为正的种子。

## 4. 资源与算力

- **硬件**：所有实验在NVIDIA H200 GPU（144GB内存）上执行。
- **计算时长**（单次优化运行）：
  - 单任务GCG：约1.5小时
  - 通用GCG：约21.9小时（因遍历18个训练样本，慢约14倍）
  - 单任务TAP：约0.6小时
  - 通用TAP：约2.4小时
- **注意力**：论文未明确说明使用GPU数量，但单个H200 GPU即可执行实验。TAP评估使用了GPT-5 API，未详细说明API开销。

## 5. 实验数量与充分性

- **实验数量**：主实验覆盖80个任务对×6种攻击方法×3个目标模型（Gemma仅部分）；加上消融实验（注入结构对比、GCG目标形式、随机信号、审判者准确性、攻击者能力、拒绝处理、跨模型迁移等），总计实验组数较多，统计上较充分。
- **充分性评价**：
  - **优点**：在多样的领域和模型上评估，消融实验全面，覆盖了攻击变体、目标公式、转移性、攻击者能力等关键因素。
  - **局限性**：受计算成本限制，仅评估AgentDojo子集（80/900+任务对），可能未覆盖完整多样性。Gemma3-4B因缺少兼容的聊天模板而未进行TAP评估。跨模型迁移仅使用Qwen3-4B作为源模型。
- **公平性**：通过确定性检查函数避免了LLM作为审判者的不可靠性；TAP的审判者准确性单独分析。ASR和S@N结合使用，考虑了随机性和多次尝试。消融实验控制变量。

## 6. 论文的主要结论与发现

1. **黑盒TAP显著优于白盒GCG**：在Qwen3-4B上，单任务/通用TAP的ASR分别为44.6%/45.2%，而GCG为23.0%/24.1%。差距源于GCG的令牌级优化在离散语义空间中不稳定，而TAP利用语境语义搜索。
2. **目标模型脆弱性差异大**：开源模型（Qwen3-4B）最脆弱，GPT-5最鲁棒（TAP ASR仅~5%）。鲁棒性与工具跟随能力存在权衡：更强的模型也更易被利用。
3. **通用攻击表现良好**：通用TAP在Qwen上ASR与单任务相当，在GPT-5上S@N更高（30% vs 14%），表明对鲁棒模型能覆盖更广泛脆弱点。
4. **攻击者模型能力至关重要**：使用GPT-5作为攻击者相比GPT-5-mini，通用TAP的ASR提升4.2-11.8倍，表明通用优化对攻击者能力要求更高。
5. **跨模型迁移困难**：GCG在Qwen家族内部保留24-36% ASR，但跨家族（GPT-5、Claude、Gemini）几乎失效（<2%，Gemini 2.5 Flash最高7.7%）。
6. **梯度引导价值有限**：随机信号GCG表现与标准GCG相当，表明梯度在离散优化中助益不大。
7. **成功注入的结构差异**：GCG产生高困惑度令牌噪声，TAP产生语义连贯的社会工程策略（如权威模仿、条件前置），在GPT-5上需要利用式策略（嵌入合法文档格式）。
8. **Slack域最脆弱，Workspace域最鲁棒**。

## 7. 优点

- **系统集成与适配**：将两种主流攻击范式（白盒/黑盒）适配到真实的智能体环境，并扩展了AgentDojo框架以支持梯度优化。
- **评估维度全面**：覆盖单任务/通用、多种模型、跨域迁移、攻击者能力消融、审判者可靠性分析等。
- **确定性评测**：使用AgentDojo的环境状态检查而非LLM文本匹配，避免评测噪音。
- **消融研究深入**：对GCG注入结构、目标形式、随机信号、TAP拒绝处理、攻击者模型变体等都进行了控制实验。
- **跨模型迁移分析广泛**：测试了8个目标模型（包含Qwen、GPT、Claude、Gemini系列），揭示了模型族特异性。
- **定性分析丰富**：详细描述了成功注入的语义模式（强制型 vs 利用型），为防御提供洞察。

## 8. 不足与局限

- **Benchmark覆盖有限**：仅使用80个任务对（子集），可能未完全代表AgentDojo的全部多样性；通用训练集仅18个任务对，规模较小。
- **模型覆盖不完整**：未评估Gemma3-4B在TAP下的表现（因兼容性问题）；未评估更多开源模型（如Llama、Mistral）。跨模型迁移仅从Qwen3-4B出发，未尝试联合优化多个源模型。
- **审判者依赖LLM**：TAP使用的LLM-as-Judge（GPT-5-mini）在Qwen上精度仅52.3%，引入测量噪声，可能误导搜索。作者指出更优的审判者能提升攻击效果。
- **计算成本约束的单次优化**：GCG通用优化需21.9小时，可能限制了搜索深度；但多次重启（S@N）部分弥补。
- **实际场景简化**：威胁模型假设攻击者完全了解系统设计，且注入点总是出现在工具输出中；真实场景可能更多样。
- **未评估防御**：论文聚焦攻击评估，未测试现有防御（如CausalArmor、AgentSentry）的反制效果，但提供了未来基准。
- **伦理考量**：作者声明在沙箱环境中进行，但方法本身可能被滥用，需明确限制条件。

（完）
