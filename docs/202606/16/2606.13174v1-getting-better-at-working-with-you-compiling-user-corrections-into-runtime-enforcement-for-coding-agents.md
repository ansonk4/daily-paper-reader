---
title: "Getting Better at Working With You: Compiling User Corrections into Runtime Enforcement for Coding Agents"
title_zh: 与您协作更好：将用户修正编译为编码代理的运行时强制执行
authors: "Yujun Zhou, Kehan Guo, Haomin Zhuang, Xiangqi Wang, Yue Huang, Zhenwen Liang, Pin-Yu Chen, Tian Gao, Nuno Moniz, Nitesh V. Chawla, Xiangliang Zhang"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13174v1"
tags: ["query:agent-safety"]
score: 8.0
evidence: 通过将用户修正编译为运行时检查来处理智能体记忆安全；在Mem0记忆系统上评估
tldr: "交互式LLM代理未能从用户纠正中持续学习，导致同一问题反复出现。针对这一缺陷，本文提出TRACE方法，在编码代理运行时中挖掘用户纠正，将其重写为原子规则并编译为强制性检查。在ClawArena和MemoryArena任务上，TRACE将偏好违反率从100%分别降至37.6%和60.5%，显著优于记忆基线。该方法通过将纠正编译为运行时约束，有效减少了用户重复纠正的需求。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13174v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1708, \"height\": 965, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13174v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1697, \"height\": 614, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13174v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1697, \"height\": 948, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13174v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1691, \"height\": 935, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13174v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1690, \"height\": 932, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13174v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 792, \"height\": 938, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13174v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 782, \"height\": 901, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13174v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1172, \"height\": 156, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13174v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1685, \"height\": 1147, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13174v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1692, \"height\": 756, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13174v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1398, \"height\": 303, \"label\": \"Table\"}]"
motivation: 用户对LLM代理的纠正无法跨会话持久生效，记忆系统仍有大量偏好违反，亟需更可靠的机制。
method: TRACE通过挖掘用户纠正、重写为原子规则并编译为运行时检查，在代理完成任务前强制执行。
result: "在ClawArena上，TRACE将偏好违反率从100%降至37.6%（分布内）和2.0%（分布外）；在MemoryArena上降至60.5%。"
conclusion: 将用户纠正编译为运行时强制执行可解决记忆无法可靠处理的重复摩擦问题，减少用户重复纠正。
---

## 摘要
交互式LLM代理正在成为日常工作的组成部分，但它们不会随着时间推移而可靠地变得更容易协作：在一个会话中记住的修正可能在下一次会话中仍被违反。我们研究了偏好获取与偏好合规之间的差距。在源自匿名真实用户摩擦案例的任务中，Mem0记忆仍有57.5%的适用偏好检查被违反。我们引入了测试时规则获取与编译执行（TRACE），这是一种即插即用的技能层管道，用于编码代理运行时，它挖掘用户修正、将其重写为原子规则，并将其编译为运行时检查，代理在完成未来任务之前必须通过这些检查。与开发者提前编写的运行时检查不同，TRACE技能来自用户自己的聊天修正。我们在ClawArena编码代理任务和源自MemoryArena的记忆密集型任务上使用模拟用户参与实验评估了TRACE。在ClawArena上，TRACE将分布内任务的保留偏好违反率从100.0%降至37.6%，将分布外任务的违反率从100.0%降至2.0%。在源自MemoryArena的任务上，TRACE将分布内违反率从100.0%降至60.5%，同时在任务通过率上匹配或超过最强的记忆基线。这些结果表明，将修正编译为运行时强制执行可以解决一种仅靠记忆无法可靠解决的重复摩擦失败模式，从而减少用户在后续会话中重复相同修正的需求。实验代码可在https://github.com/YujunZhou/TRACE_exp获取，可部署技能可在https://github.com/YujunZhou/tellonce获取。

## Abstract
Interactive LLM agents are becoming part of daily work, but they do not reliably become easier to work with over time: a correction remembered in one session may still be violated in the next. We study this gap between preference access and preference compliance. In tasks derived from anonymized real-user friction cases, Mem0 memory still leaves 57.5% of applicable preference checks violated. We introduce Test-time Rule Acquisition and Compiled Enforcement (TRACE), a drop-in skill-layer pipeline for coding-agent runtimes that mines user corrections, rewrites them as atomic rules, and compiles them into runtime checks that must pass before an agent completes future tasks. Unlike runtime checks written ahead of time by developers, TRACE skills come from the user's own chat corrections. We evaluate TRACE with simulated user-in-the-loop experiments on ClawArena coding-agent tasks and MemoryArena-derived memory-intensive tasks. On ClawArena, TRACE reduces held-out preference violation from 100.0% to 37.6% on in-distribution tasks and from 100.0% to 2.0% on out-of-distribution tasks. On MemoryArena-derived tasks, TRACE reduces in-distribution violation from 100.0% to 60.5% while matching or exceeding the strongest memory baseline on task pass. These results suggest that compiling corrections into runtime enforcement can address a repeated-friction failure mode that memory alone does not reliably solve, reducing the need for users to restate the same correction across future sessions. Experiment code is available at https://github.com/YujunZhou/TRACE_exp, and the deployable skill is available at https://github.com/YujunZhou/tellonce.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容所生成的详细中文总结：

### 论文核心问题与整体含义

- **研究动机与背景**：交互式大型语言模型（LLM）代理（特别是编码代理）在用户日常使用中逐渐普及。一个核心期望是：代理应能随着用户的修正反馈而持续进步，避免用户在跨会话场景中重复相同的纠正。然而，现有系统存在一个关键问题：**即使代理记住了用户的偏好（例如通过记忆系统），也并不能保证在未来的任务中会遵守这些偏好**。论文将这种现象定义为“**获取-合规差距**”（access-compliance gap）。即，偏好被检索（获取）与代理实际执行（合规）之间存在巨大的鸿沟，导致用户需要反复纠正同一问题，体验较差。

### 论文提出的方法论：Test-time Rule Acquisition and Compiled Enforcement (TRACE)

- **核心思想**：TRACE 是一个即插即用的技能层（skill-layer）管道，旨在解决上述差距。其核心是将用户非结构化的自然语言修正，转换为**运行时（runtime）可强制执行的规则**。与仅仅将修正作为“建议性文本”存入记忆不同，TRACE 将用户偏好编译成在代理完成任务前必须通过的检查，从而将其从“被动建议”转变为“主动执行约束”。

- **关键技术细节**：
    - **流程概览**：TRACE 管道包括四个主要阶段：
        1.  **在线扫描 (Inline Scan)**：实时分析用户输入，检测是否包含“修正信号”（如明确的偏好、对重复错误的通知）。
        2.  **编译器 (Compiler)**：如果检测到信号，则进行以下操作：
            - **记录 (Record)**：捕获修正相关的反馈、代理行为和环境上下文。
            - **原子化 (Atomize)**：从修正中推导出原子规则（如“在停止前清理调试文件”）及其适用条件（如仅当代理创建了临时文件时）。
            - **编译 (Compile)**：将规则转化为可执行的强制执行构件。
        3.  **规则库 (Per-user Rule Library)**：维护一个不断增长的用户规则库，并采用生命周期管理（Lifecycle Management）来处理规则间的冲突、重复或细化。
        4.  **运行时强制执行 (Runtime Enforcement)**：在未来的任务中，将匹配的规则作为“运行时门控”加载，代理的执行（如文件写入、命令执行）受到监控，只有通过所有已验证的检查后，任务才能完成。

    - **规则生命周期**：为处理新规则与旧库的交互，TRACE 实现了五种决策操作：**无操作 (Noop)**（更新证据）、**更新 (Update)**（细化规则）、**取代 (Supersede)**（冲突时停用旧规则）、**分割 (Split)**（拆分复杂修正）和**新建 (New)**（创建新规则）。

    - **强制执行层级**：TRACE 实现了三个层级来应对不同复杂度的规则：**确定性 (Deterministic)** 规则（通过工具调用、文件名等验证）、**语义 (Semantic)** 规则（需要模型评估生成文本）、**意图 (Intent-level)** 规则（作为运行时提醒）。

### 实验设计

- **数据集与Benchmark**：
    - **诊断性数据集 (来自论文Section 3)**：从约两个月、32个长文本编码代理会话中，人工提取了142个修正冲突记录，筛选出19个保留评估任务（包含29个偏好检查），用于衡量“获取-合规差距”。
    - **主要Benchmark**：
        - **ClawArena**：多轮编码任务基准，包含62个场景模板。分为32个训练场景（用于派生规则）和30个分布外（OOD）场景。
        - **MemoryArena（改编版）**：原为代理记忆基准，论文通过添加用户/项目约束和修正机会的交互包装器来适配任务，使其能评估偏好遵循。

- **对比方法**：
    - **无记忆 (No Memory)**：基线，无任何偏好信息。
    - **Mem0 记忆**：一个先进的长期记忆系统。
    - **Hindsight 记忆**：另一种基于反思的记忆方法。
    - **ReMe-Light 记忆**：一个动态过程记忆框架。
    - **诊断实验中的变体**：`All Rules`（所有规则作为提示）、`Relevant Rules`（相关规则作为提示）、`Compiled Rules`（TRACE生成的编译后规则）。

- **实验设置与指标**：采用模拟用户（Simulated User）参与协议，模拟用户在交互中发出修正。评估时冻结训练状态，确保代理仅依赖先前获取的规则。主要指标包括：**任务通过率 (Task Pass)**、**违规率 (Violation Rate)**（越低越好）、**平均修正次数 (Mean Corrections)**（越低越好）。

### 资源与算力

- 论文中**没有明确说明**使用的GPU型号、数量和训练/推理时长。文中仅提及使用了轻量级LLM（Gemma 4 31B）用于检测和编译，并且评估了运行时效率（ClawArena上TRACE平均每轮约42秒，优于大多数记忆基线）。算力信息不属于论文公开披露的重点。

### 实验数量与充分性

- **实验数量**: 论文进行了较为充分的对比实验，包括：
    - **1个诊断性实验**：在6个模型上比较5种条件，验证“获取-合规差距”的存在。
    - **2个主流基准测试**：在ClawArena和MemoryArena上，使用4个主流编码Agent（如Claude Code, Codex CLI）进行ID和OOD评估。
    - **1个效率分析**：分析TRACE在用户交互轮次和墙钟时间上的成本。
    - **模拟器验证**：对模拟用户（Simulated User）的保真度进行了量化评估（精确率、召回率、F1等）。

- **实验充分性与公平性**：
    - **充分**：实验覆盖了ID和OOD场景，评估了任务成功率和偏好遵循率两个维度，并与多个先进的记忆基线进行对比。诊断实验设计系统地隔离了不同变量（规则有无、是否编译）的影响。
    - **客观与公平**：论文采用了PersonaGym的模拟用户协议，并使用基准测试自带的客观检查器评分，减少了主观性。在OOD实验中验证了规则的泛化能力，排除了过拟合风险。实验结果清晰地显示了TRACE的显著优势。

### 论文的主要结论与发现

1.  **量化了“获取-合规差距”**：即使在最理想的情况下（将相关规则作为上下文），代理对适用偏好的合规率也仅为54.0%，编译后规则可达70.1%，而基线（无规则）仅为31.6%。这表明记忆检索本身是无效的。
2.  **TRACE显著降低偏好违反率**：
    - 在**ClawArena**上，TRACE将ID任务的违反率从100%降至37.6%，在OOD场景下更是从100%降至惊人的2.0%，且不牺牲任务完成率。
    - 在**MemoryArena**上，TRACE同样将ID任务违反率从100%降至60.5%，并获得了最高的任务通过率。
3.  **减少用户重复纠正**：TRACE 显著降低了测试时的用户交互轮次。在ClawArena的OOD场景下，用户几乎无需再次介入（平均用户轮次为1.02，接近1次任务请求）。这表明编译后的规则具有强大的泛化能力。
4.  **运行时效率高**：TRACE的运行时间与“无记忆”基线相当，远低于ReMe-Light等记忆系统，说明其带来的交互收益并非以高昂的运行时开销为代价。

### 优点

1.  **问题识别精准**：清晰定义了并量化了个人化AI代理中一个普遍但未被充分解决的“获取-合规差距”，切中实际应用痛点。
2.  **方法创新**：提出将用户修正从“被动记忆”转化为“主动执行约束”的范式转移。TRACE 的端到端管线（从实时检测、规则编译、生命周期管理到运行时门控）设计精巧、自洽。
3.  **实验设计扎实**：诊断实验、ID/OOD测试、效率分析和模拟器验证，构建了一个多角度的评估立体结构，增强了结论的可信度。在多个模型和基准上进行评估，展示了方法的鲁棒性。
4.  **结果惊艳**：在ClawArena OOD任务中实现2.0%的极低违反率，这是一个非常突出的结果，有力地证明了编译执行的有效性和泛化能力。
5.  **实用性高**：强调作为即插即用的技能层，可与现有编码代理运行时集成，并且其运行时开销与无记忆基线相当，具有实际部署价值。

### 不足与局限

1.  **对LLM编译器的依赖与偏差风险**：整个TRACE流程依赖一个轻量级LLM进行信号检测、规则提取和编译。虽然文中提及了自我验证环来减少幻觉，但编译器本身仍可能引入偏差。例如，错误地将普通反馈识别为修正，或提取出不准确的规则。
2.  **生命周期管理中的局限性**：在规则取代方面，系统缺乏自动回滚保护，错误的取代决策需要人工审计和手动恢复。在库规模增大时，这可能会成为运维负担。
3.  **上下文依赖性**：规则的应用依赖于任务与规则适用条件的匹配。在OOD的MemoryArena任务中，TRACE的违规率仍高达97.0%，论文指出这主要是因为OOD任务暴露了大量未被规则覆盖的约束，凸显了方法对历史数据的依赖性。
4.  **用户数量限制**：诊断数据集来源于**单一用户（一位AI研究员）** 的交互记录，可能缺乏不同用户修正模式的多样性。虽然模拟用户被用来扩大实验，但其保真度（特别是规则召回率0.668）仍有提升空间。
5.  **可扩展性**：虽然库规模较小，但规则数量增长后的管理（如冲突解决、检索效率）可能成为挑战。论文也未讨论规则在多个不同用户间（隐私隔离）的部署。

（完）
