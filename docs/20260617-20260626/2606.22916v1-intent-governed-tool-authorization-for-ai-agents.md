---
title: Intent-Governed Tool Authorization for AI Agents
title_zh: 為AI代理而設嘅意圖主導工具授權機制
authors: "Genliang Zhu, Chu Wang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22916v1"
tags: ["query:harness"]
score: 8.0
relevance_score: 8.0
author_score: 8.0
author_affiliations: "Genliang Zhu (first_author): Accentrust, Georgia Institute of Technology; Chu Wang (last_author): Accentrust, University of Illinois Urbana-Champaign"
author_rating_explanation: "Focus authors are affiliated with Georgia Institute of Technology and the University of Illinois Urbana-Champaign, both top US AI schools in the 8-9 band. The additional Accentrust affiliation does not raise the score beyond the verified top-school evidence."
evidence: intent-governed tool authorization for AI agents
tldr: "AI agents invoke external tools under static credentials, yet such credentials may grant broader authority than a user's current request justifies, enabling unjustified operations like exporting records when only a summary was requested. This paper proposes Intent-Governed Access Control (IGAC), a server-side authorization layer that treats user intent as a monotone, auditable policy attribute narrowing tool-call authority. IGAC introduces intent certificates, session-scoped policy narrowing, intent-aware manifest filtering, and intent-tool-payload consistency checks mapped onto the OpenPort governance substrate. The approach ensures intent only reduces static integration authority without expanding scopes, data policy, tenant boundaries, or review requirements。"
source: arxiv
selection_source: fresh_fetch
motivation: "Static integration credentials can authorize tool calls unjustified by the user's current request, creating a gap between granted authority and legitimate intent。"
method: "IGAC introduces intent certificates, session-scoped policy narrowing, intent-aware manifest filtering, and intent-tool-payload consistency checks as a server-side authorization layer。"
result: "IGAC is mapped onto OpenPort, implementing intent-governed authorization-dependent discovery, scope checks, and ABAC-style policy enforcement with stable reason codes and audit。"
conclusion: "Treating user intent as a monotone, audifiable policy attribute enables fine-grained, justified AI-agent tool use without expanding static integration authority。"
---

## Chinese Abstract
AI代理越嚟越依賴外部工具嚟執行操作：佢哋會讀取私人數據、構建結構化負載、提交寫入請求、匯出記錄，並協調跨應用嘅工作流程。現有嘅授權機制通常只會問一個整合憑證、應用程式或權杖有冇權去呼叫某個工具。呢個問題係必要，但係唔夠全面。一個工具呼叫可能已經透過靜態憑證授權，但係仍然唔符合用戶目前嘅請求。例如，一個可以讀取同匯出記錄嘅憑證，唔應該喺用戶只要求有限度摘要嘅時候暴露匯出權限；而模型產生嘅刪除呼叫亦唔應該純粹因為整合擁有刪除範圍就執行。本文提出「意圖主導存取控制」（Intent-Governed Access Control，簡稱IGAC），係一個伺服器端嘅授權層，將用戶表達嘅意圖視為一個單調、可審計嘅政策屬性，用喺AI代理嘅工具使用上。IGAC引入意圖證書、會話範圍嘅政策收窄、感知意圖嘅清單過濾，以及意圖—工具—負載嘅一致性檢查。其核心不變量係：用戶意圖只能夠縮減靜態整合政策所授予嘅權限，佢永遠唔會擴展範圍、數據政策、租戶邊界或審查要求。我哋將IGAC映射到OpenPort——一個現有嘅治理底層，已經實現授權依賴嘅探索、範圍同ABAC風格嘅政策檢查、草稿優先寫入、預飛行影響綁定、狀態見證檢查、冪等性、穩定嘅原因碼同審計。

## Abstract
AI agents increasingly act through external tools: they read private data, construct structured payloads, submit write requests, export records, and coordinate workflows across application boundaries. Existing authorization mechanisms usually ask whether an integration credential, app, or token can call a tool. That question is necessary but incomplete. A tool call can be authorized by static credentials and still be unjustified by the user's current request. For example, a credential that can read and export records should not expose export authority when the user only asked for a bounded summary, and a model-generated delete call should not execute merely because the integration has a delete scope. This paper proposes Intent-Governed Access Control (IGAC), a server-side authorization layer that treats the user's expressed intent as a monotone, auditable policy attribute for AI-agent tool use. IGAC introduces intent certificates, session-scoped policy narrowing, intent-aware manifest filtering, and intent-tool-payload consistency checks. The central invariant is that user intent may only reduce the authority granted by static integration policy; it never expands scopes, data policy, tenant boundaries, or review requirements. We map IGAC onto OpenPort, an existing governance substrate that already implements authorization-dependent discovery, scope and ABAC-style policy checks, draft-first writes, preflight impact binding, state-witness checks, idempotency, stable reason codes, and audit.