---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: AutoSpec：透過歸納邏輯編程實現 LLM 代理安全規則進化
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v2"
tags: ["query:harness"]
score: 7.8
relevance_score: 10.0
author_score: 4.5
author_affiliations: "Pingchuan Ma (first_author): Zhejiang University of Technology; Xiaoqin Zhang (last_author): Zhejiang University of Technology"
author_rating_explanation: "Both focus authors are affiliated with Zhejiang University of Technology. This is a Chinese university but not among the top-tier Chinese institutions listed in the 6-8 band (Tsinghua, Peking, SJTU, Zhejiang, USTC, CAS, etc.), so it falls in the 0-5 bucket."
evidence: framework that evolves safety rules for LLM agents
tldr: "LLM agents executing complex tasks face safety risks, but existing safety rules are either brittle hand-crafted rules with high false positives/negatives or opaque neural classifiers. AutoSpec automatically evolves expert-designed safety rules from user annotations using counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). ILP efficiently identifies discriminative predicates to prune the exponential search space of rule edits, iteratively refining rules until convergence. Evaluated on 291 traces across code execution and embodied agent domains, AutoSpec raises rule F1 to 0.98 and 0.93, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations。"
source: arxiv
selection_source: fresh_fetch
motivation: "Hand-crafted safety rules for LLM agents are brittle with high false positives or negatives, while neural classifiers lack interpretability required for safety-critical deployments。"
method: "AutoSpec uses CEGIS guided by ILP to iteratively mine counterexamples from annotated traces, learn discriminative predicates, generate candidate rule edits, and verify to select optimal revisions。"
result: "AutoSpec achieves F1 of 0.98 and 0.93 on code execution and embodied agent domains, reducing false positives by up to 94% with 4.8x higher F1 than heuristic CEGIS。"
conclusion: "AutoSpec produces interpretable, auditable safety rules that balance precision and recall, generalizing to unseen scenarios for safer LLM agent deployment。"
---

## Chinese Abstract
大型語言模型（LLM）代理透過整合語言模型同外部工具同環境，愈嚟愈多自動化處理複雜任務。然而，佢哋嘅自主性帶嚟重大安全風險：代理可能會執行破壞性指令、洩露敏感數據，或者違反領域限制。現有安全方法面對一個根本性取捨：手製規則雖然可以解釋但好死板，過度保守嘅規則會阻斷安全操作（假陽性過高），而過於寬鬆嘅規則又會漏掉不安全行為（假陰性過高）。神經分類器亦缺乏安全關鍵部署所需嘅可解釋性。我哋提出 AutoSpec，一個透過反例引導歸納綜合（CEGIS）配合歸納邏輯編程（ILP），從用戶嘅安全／不安全標註自動進化專家設計嘅已部署安全規則嘅框架。由專家規則同標註追蹤數據流開始，AutoSpec 會迭代評估規則、挖掘假陽性和假陰性反例、用 ILP 學習邊啲謂詞可以區分呢啲反例、生成候選規則修改，再驗證候選修改去揀最佳修訂。關鍵洞察係 ILP 能夠高效識別喺假陰性中經常出現但喺假陽性中少見嘅謂詞（或者反過來），從而大幅修剪規則修改嘅指數級搜尋空間。呢個過程會持續到收斂為止，最終產生可以解釋、能夠平衡精確度同召回率嘅規則。我哋喺 291 個執行追蹤上面評估 AutoSpec，涵蓋代碼執行同具身代理兩個領域。AutoSpec 將規則 F1 分數提升到 0.98 同 0.93，假陽性最多減少 94% 同時保持高召回率，並喺 4–5 次迭代內收斂。ILP 引導嘅方法相比啟發式 CEGIS，F1 分數最多提升 4.8 倍。學到嘅規則係人類可讀、可審計嘅，而且能夠泛化到未見過嘅場景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.