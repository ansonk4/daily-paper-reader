---
title: "Layer-Isolated Evaluation: Gating the Deterministic Scaffold of a Production LLM Agent with a No-LLM, Regression-Locked Test Harness"
title_zh: 分层隔离评估：使用无LLM、回归锁定的测试框架对生产级LLM代理的确定性脚手架进行门控
authors: "Sawyer Zhang, Alexander Wang, Sophie Lei"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11686v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 对代理记忆和安全层进行分层隔离评估
tldr: 现有LLM智能体评估依赖端到端任务成功率，单指标无法定位回归。本文提出层级隔离评估，将智能体分解为8个固定层次，每层用无LLM的确定性测试切片独立验证（238条，2.39秒运行）。注入单层回归后，聚合通过率仅降1.7-5.9pp，而匹配切片降25-91pp，定位准确（平均排名1.29/19）。该方法实现了组件级评估的确定性实例，弥补了EDDOps的空白，并在双租户上验证了可迁移性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11686v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 510, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11686v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1137, \"height\": 414, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11686v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1708, \"height\": 565, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11686v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1288, \"height\": 1168, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11686v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1525, \"height\": 519, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11686v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1305, \"height\": 413, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11686v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1104, \"height\": 371, \"label\": \"Table\"}]"
motivation: 现有端到端指标无法定位LLM智能体回归错误，需细粒度评估方法。
method: 将智能体分解为8层，每层用无LLM的确定性测试切片独立验证，CI中基线锁定。
result: 注入单层回归后，聚合通过率仅降1.7-5.9pp，而对应切片降25-91pp，7/7案例定位准确。
conclusion: 层级隔离评估精确定位回归，是组件级评估的确定性实例，在双租户上验证可迁移。
---

## 摘要
端到端任务成功是评估LLM代理的主要方式，但一个聚合数字只能告诉你代理出现了回归，而无法告知具体位置。我们提出了分层隔离评估：将一个已部署的排序代理分解为固定的分层分类法（本体、意图、路由、分解、升级、安全、记忆和跨领域的封装/防御），每一层都由其自身的断言切片在确定性、无LLM的“纯”模式下执行。纯测试套件（23个切片共238个案例；225个案例在2.39秒内运行，约10毫秒/案例）在每次变更时针对锁定的每切片基线在CI中运行。我们通过控制回归注入进行验证，一次退化一个非安全层（共七个非安全层）。我们未设计的效果是掩蔽：聚合通过率几乎不变（六次局部回归变化范围为-1.7至-5.9个百分点），而匹配的切片则大幅下降（-25至-91个百分点）。层切片对其自身故障的反应部分由构造决定；测量结果是（i）聚合掩蔽效应，以及（ii）损害不会扩散到其他切片：注入层的切片在7个案例中有5个是唯一受影响最严重的，在7个案例中全部进入前三（在19个切片中平均排名1.29）。定位在第二个结构不同的租户（星巴克新加坡）上复现：所有七个匹配切片均大幅下降，因此这不是单一目录的伪影。我们将其定位为EDDOps规定但未实现的组件级评估的具体确定性实例化，以CheckList为前身，并作为全工作流随机变异测试的确定性镜像。我们的贡献包括：（a）为生产级代理提供完全分解、亚秒级、无LLM的每层测试框架，（b）一种诚实覆盖的测试充分性标准，拒绝给未执行的层打分，以及（c）回归注入演示，证明每切片基线锁定门控能够定位聚合指标所掩盖的回归。

## Abstract
End-to-end task-success is the dominant way to evaluate LLM agents, but one aggregate number tells you that an agent regressed, not where. We present layer-isolated evaluation: a deployed ordering agent is decomposed into a fixed taxonomy of layers (ontology, intent, routing, decomposition, escalation, safety, memory, and cross-cutting envelope/defense), each exercised by its own assertion slice in a deterministic, no-LLM "pure" mode. The pure suite (238 cases across 23 slices; 225 run in 2.39 s, ~10 ms/case) runs in CI on every change against a locked per-slice baseline. We validate by controlled regression injection, degrading one layer at a time across seven non-safety layers. The effect we did not design in is masking: the aggregate pass-rate barely moves (-1.7 to -5.9 pp for six local regressions), while the matching slice craters (-25 to -91 pp). A layer's slice reacting to its own fault is partly by construction; the measured results are (i) the aggregate masking and (ii) that damage stays off the other slices: the injected layer's slice is the single worst-hit in 5 of 7 cases and top-3 in 7 of 7 (mean rank 1.29 of 19). Localization replicates on a second, structurally different tenant (Starbucks SG): all seven matching slices crater, so it is not a single-catalog artifact. We position it as a concrete, deterministic instantiation of the component-level evaluation EDDOps prescribes but leaves unimplemented, with CheckList as ancestor and as the deterministic mirror image of whole-workflow stochastic mutation testing. Our contributions: (a) a fully decomposed, sub-second, no-LLM per-layer harness for a production agent, (b) a coverage-honesty test-adequacy criterion that refuses to score an unexercised layer, and (c) the regression-injection demonstration that per-slice baseline-locked gates localize regressions an aggregate metric masks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前LLM代理评估主要依赖端到端任务成功率，该单一聚合指标能检测到代理出现“回归”（性能下降），但无法定位问题出在哪个子系统（如意图解析、规划、升级、安全验证等），且重新运行随机、分钟级的代理进行二分查找耗时且噪声大。
- **研究动机**：已有工作（如Xia等人提出的EDDOps、Mazaheri等人的AgentAtlas）指出需要组件级评估，但缺乏具体可运行的确定性实例化。本文旨在提供一个针对真实部署代理的完全分解、可运行、确定性且廉价的评估方法，以弥补端到端指标无法定位回归的缺陷。
- **整体含义**：通过将代理的确定性“脚手架”分解为固定层次，每层用无LLM的断言切片进行纯模式测试，从而实现亚秒级的每PR门控，并能精确指示回归所在层，避免聚合指标对回归的掩蔽。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：**分层隔离评估**——将生产级LLM代理分解为8个固定架构层次，每层定义一个断言切片，在**无LLM的纯模式**下运行（仅依赖确定性规则、字典查找等），在每次代码变更时（CI）对比锁定的每切片基线执行门控。
- **关键技术细节**：
  - **分解结构**：包含8个层次（L0本体预解析、意图信号、言语行为；L2工具路由；L3子目标分解、约束处理、升级；L4安全（价格/SKU/过敏原）、知识、记忆；加上跨领域：封装、防御、OOD拒绝、改写器、语言环境保真度、会话初始化等），共23个切片，其中19个有测试用例，4个无用例时报告为`null`（未覆盖）。
  - **纯模式**：每个断言切片仅验证确定性输出，无需LLM调用（如本体解析器的规范ID、基于规则的升级决策、改写器的字典重写、OOD短路谓词等）。单用例约1毫秒计算，完全可复现。
  - **锁定基线**：冻结基线记录每个切片的（总数、通过数、通过率、失败ID）。当前基线238个用例，23个切片，通过率100%。关键规则：无用例的切片报告`rate: null`（未覆盖），从不报告1.0，以防止绿数掩盖未测试的层。
  - **门控流程**：每次PR运行纯套件（约225个用例，2.39秒），对比基线；任何切片通过率下降则阻止合并；新出现的未覆盖切片本身也视为失败。
  - **算法/流程**：论文给出简化伪代码（Listing 1）：加载基线 → 运行纯套件 → 遍历所有切片：若基线通过率为空则跳过（仍未覆盖）；若当前通过率低于基线则阻止合并。每个断言是简单的确定性断言（如`assert observed.total_cents == expected`）。

## 3. 实验设计：使用了哪些数据集/场景，benchmark是什么，对比了哪些方法

- **数据集/场景**：基于一个真实生产级的食品饮料点单代理（参考租户kopitiam），以及第二个结构不同的租户（星巴克新加坡）。使用作者编写的断言切片套件（238个用例，23个切片；星巴克新加坡独立编写16个用例覆盖7个注入层）。
- **Benchmark**：以锁定基线（当前行为）为基准，无外部标准benchmark。通过**控制回归注入**验证定位能力：为7个非安全层分别注入单层退化（如升级从不触发、意图信号清空、防御放行所有、OOD从不拒绝、改写器恒等、分解器不产生子目标、本体解析清空），然后运行整个纯套件，观察聚合通过率变化与每切片变化。
- **对比的方法**：
  - 与**端到端聚合指标**对比（本文的核心对比：聚合通过率几乎不变 vs 匹配切片大幅下降，显示掩蔽）。
  - 与**全工作流随机变异测试**（Bhardwaj 2026, AgentAssay）对比，强调差分：本文是确定性、每层、无LLM、每PR门控；AgentAssay是随机、全工作流、含LLM、采样/nightly。两者互补。
  - 与**行为测试框架CheckList**（Ribeiro等人）对比，本文是其思想在代理内部层的应用。
  - 未与其他分解方法进行直接实验对比，因为其他方法（如AgentBoard、APB）仅分解单一能力而非整个代理。

## 4. 资源与算力

- 文中明确说明：**全纯测试套件（225个用例）在2.39秒内运行**（包括进程启动和夹具），平均约10毫秒/用例。**不调用任何LLM模型**，因此**无需GPU**，无Token成本。相比之下，一次实时多轮代理中位延迟为73秒（p95 192秒），且需支付LLM费用。本文方法的经济优势在于近乎零成本，可实现每PR门控（而非夜间任务）。
- 未提及任何GPU型号、数量或训练时长，因为完全不涉及模型训练或推理。

## 5. 实验数量与充分性

- **注入实验**：对7个非安全层进行了7次单层回归注入（6个局部回归+1个基础层回归），每次重复记录聚合通过率、切片通过率、受影响的切片数、排名。结果确定性（无噪声），因此单次运行即为精确结果。
- **跨租户复现**：在星巴克新加坡租户上完整复现上述7次注入，共7个独立实验，验证定位可迁移性。
- **有机回归测试**：对代理历史中两个真实修复的回归状态进行回放（OOD过度拒绝、跨轮指代失效），发现当前纯套件未覆盖它们，因此正确报告无影响（诚实覆盖）。
- **消融/验证**：实验中还包括对测试框架自身状态的检查（如每轮重建运行时防止状态泄漏），通过比较同一基线两次运行确认无假阳性。
- **充分性分析**：实验覆盖了7个非安全层，但未覆盖安全层（故意不注入安全代码），且仅针对一个代理（点单代理）和两个租户。作者承认注入故障是作者选择的，非有机发现；测试套件仍有4个切片未覆盖，2个切片用例极少。总体而言，实验证明了方法在受控条件下的定位能力和跨租户外推性，但尚未证明对有机、未预见回归的鲁棒性（需进一步研究）。

## 6. 论文的主要结论与发现

- **掩蔽效应**：聚合纯套件通过率在6个局部回归注入下仅下降1.7–5.9个百分点，小到可被仪表盘噪声淹没；而对应的切片大幅下降25–91个百分点。基础层回归（本体）聚合下降26.5pp，但匹配切片下降95pp，仍显著。
- **精确定位**：注入层的切片在7次中有5次是受影响最严重的切片（Top-1），全部进入Top-3，平均排名1.29/19。非目标切片几乎不动（近扁平非对角），证明故障被局部化。
- **基础层 vs 局部层**：基础层（本体、意图）故障具有更宽的影响范围（辐射多个下游切片），但切片排名中注入层仍接近最高（Top-2），且这种扩散本身就是有用信号（标识回归是基础性的而非局部的）。
- **跨租户复现**：在星巴克新加坡租户上，所有七次注入的匹配切片均大幅下降（–50至–100pp），且6/7次非对角干净，唯一例外是本体故障（同样影响下游）。确认定位结果不是单一目录伪影。
- **诚实覆盖的必要性**：通过历史真实回归案例（确认门）展示：即使当前覆盖不足，诚实报告未覆盖切片可暴露盲点。关闭未覆盖切片可将该回归类转化为每PR阻塞。

## 7. 优点：方法或实验设计上的亮点

- **确定性 + 亚秒速度**：无LLM调用使测试完全可复现、低成本（2.39秒全套件），实现每PR硬门控，而非缓慢的夜间跑批。
- **分解层次清晰**：将代理行为分解为固定层次，每层对应一个单独断言切片，使故障可精确定位到修复层。
- **诚实覆盖标准**：零用例切片报告为`null`而非100%，防止绿数掩盖未测试层，并作为测试套件本身的质量信号（暴露缺口）。
- **验证方法严谨**：通过单层回归注入 + 自验证（确保注入生效）+ 清理运行时状态避免假耦合 + 跨租户复现，实验结果可靠。
- **现实案例附注**：提供历史真实回归的讨论，展示覆盖诚实的价值，即使当前未覆盖。
- **经济论点清晰**：与实时评估的对比表（纯模式 vs 实时）直观说明成本优势。
- **明晰的定位声明**：不宣称发明组件级评估，而是提供具体实例化；明确声明纯套件是漂移检测器而非正确性预言机；诚实地指出注入实验的“构造性”部分和“测量性”部分。

## 8. 不足与局限

- **覆盖范围有限**：4个切片（路由、记忆、个性化、反射）目前未覆盖，2个切片（健康、语言环境保真度）用例数极低（N=1或3）。门控仅能覆盖已写测试的层。
- **注入故障为作者选择**：7次注入均为人工设计的退化，未使用有机、未预见的回归。虽然复制在第二租户上，但未证明能定位真实开发中发生的偶然故障。
- **测试套件是漂移检测器，非正确性预言机**：基线锁定当前行为，若基线编码错误行为，门控会锁住错误。门控保证“未变更”，不保证“正确”。
- **仅适用于点单代理类型**：方法架构（层次分类法）高度适应F&B点单领域，是否可推广到其他领域（如通用助手、编程代理）未验证。
- **跨租户复现的掩蔽幅度未验证**：星巴克租户的测试套件较小（16个用例），注入导致的聚合下降幅度较大（–6至–38pp），掩蔽效应需要在较大套件中才能体现。因此跨租户只验证了定位，未验证掩蔽幅度。
- **有机回归回放阴性结果**：两个历史回归当前测试未覆盖，表明测试套件存在缺口，需要持续扩充。
- **缺乏与现有方法系统对比**：未在相同基准上比较定位精度、成本等（如与AgentAssay或消融方法的直接实验）。
- **安全层未注入**：作者故意不退化安全代码（即使内存中），因此该方法在安全门控上的定位能力未验证。
- **单代理框架（PydanticAI）**：依赖特定框架的实现细节，泛化性需进一步证明。

（完）
