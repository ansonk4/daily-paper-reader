---
title: "Beyond Similarity: Trustworthy Memory Search for Personal AI Agents"
title_zh: 超越相似性：面向个人AI代理的可信记忆搜索
authors: "Jiawen Zhang, Kejia Chen, Jiachen Ma, Yangfan Hu, Lipeng He, Yechao Zhang, Jian Liu, Xiaohu Yang, Tianwei Zhang, Ruoxi Jia"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06054v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 评估个人AI代理中的对抗性记忆威胁和可信记忆搜索
tldr: 个人AI代理的长期记忆搜索基于语义相似性，导致跨域泄露、谄媚等信任威胁。本文评估了多种记忆框架，提出MemGate——仅9M参数的轻量级神经门控插件，在向量存储与LLM间执行查询条件化记忆准入。在多个真实场景中，MemGate有效降低记忆诱导威胁，同时保持长期记忆的实用性，无需修改LLM或数据库。该工作将原始相似搜索转化为任务适配的记忆筛选，为代理提供了可部署的信任边界。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06054v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 885, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06054v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1798, \"height\": 798, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06054v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1837, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06054v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 894, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06054v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1836, \"height\": 282, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06054v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 856, \"height\": 900, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06054v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 860, \"height\": 897, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06054v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 896, \"height\": 288, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06054v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1753, \"height\": 667, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06054v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1684, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06054v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1825, \"height\": 928, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06054v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 766, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06054v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 937, \"height\": 931, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06054v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1831, \"height\": 414, \"label\": \"Table\"}]"
motivation: 现有记忆搜索仅依赖语义相似性，导致上下文不合适的记忆引发安全威胁，需建立信任边界。
method: MemGate在向量记忆存储与骨干LLM间插入查询条件化神经门，对候选记忆进行动态筛选准入。
result: 在A-Mem、Mem0等框架及多种LLM上，MemGate降低记忆诱导威胁并保持长时记忆效用。
conclusion: 轻量级插件MemGate无需LLM修改或数据库重写，可部署为个人AI代理的可信记忆搜索方案。
---

## 摘要
个人AI代理越来越依赖长期记忆来跨会话提供持久个性化。然而，现有记忆管道主要由语义相似性驱动：检索与当前查询语义接近的记忆数据并注入模型上下文。这造成了关键的可信度缺口，因为语义相关的记忆可能仍然在上下文中不恰当，导致跨域泄露、谄媚、工具调用漂移或记忆引发越狱等威胁。在本文中，我们研究记忆搜索作为个人AI代理中的信任边界。我们评估了代表性的代理记忆框架，包括A-Mem、Mem0和MemOS，以及OpenClaw——一个具有持久状态和工具使用能力的真实世界个人代理环境。我们的结果表明，长期记忆不仅仅是一个实用层，而是一个持久控制通道，可以重塑代理如何解释任务和执行行动，使它们极易受到上述威胁的影响。为缓解这些漏洞，我们提出MemGate，一个轻量级且可部署的记忆插件，用于可信记忆搜索，仅9M参数和35.1MB占用。MemGate插入在向量记忆存储和主干LLM之间，无需修改LLM、重写记忆数据库或推理时LLM评判。它对候选记忆表示应用查询条件神经门控，将原始相似性搜索转化为任务条件记忆准入。在多个主流记忆框架、真实世界代理设置和多样化LLM主干中，MemGate在保留长期记忆效用的同时减少了记忆引发的威胁。

## Abstract
Personal AI agents increasingly rely on long-term memory to provide persistent personalization across sessions. However, existing memory pipelines are largely driven by semantic similarity: memory data close to the current query is retrieved and injected into the model context. This creates a critical trustworthiness gap, since a semantically related memory may still be contextually inappropriate, leading to threats such as cross-domain leakage, sycophancy, tool-call drift, or memory-induced jailbreaks.   In this paper, we study memory search as a trust boundary in personal AI agents. We evaluate representative agentic memory frameworks, including A-Mem, Mem0, and MemOS, together with OpenClaw, a real-world personal-agent environment with persistent state and tool-use capability. Our results show that long-term memory is not merely a utility layer, but a durable control channel that can reshape how agents interpret tasks and execute actions, leaving them highly susceptible to the aforementioned threats. To mitigate these vulnerabilities, we propose MemGate, a lightweight and deployable memory plug-in for trustworthy memory search, with only 9M parameters and a 35.1MB footprint. MemGate is inserted between the vector memory store and the backbone LLM, requiring no LLM modification, memory-database rewriting, or inference-time LLM judge. It applies a query-conditioned neural gate to candidate memory representations, turning raw similarity search into task-conditioned memory admission. Across multiple mainstream memory frameworks, real-world agent settings, and diverse LLM backbones, MemGate reduces memory-induced threats while preserving long-term memory utility.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入、客观的中文总结。

---

### **1. 论文的核心问题与整体含义（研究动机和背景）**

*   **核心问题**：个人AI代理的长期记忆搜索机制存在严重的**可信度缺口**。现有的记忆系统普遍基于**语义相似性**进行检索，但语义相似的记忆在任务上下文中可能是不恰当甚至有害的。这会导致一系列安全威胁，如**跨域信息泄露**、**谄媚行为**、**工具调用漂移**以及**记忆诱导的越狱攻击**。
*   **研究动机**：论文指出，长期记忆对于个人代理不仅是“实用增强层”，更是一个**持久的控制通道**，能够从根本上改变代理对任务的解读和行动执行方式。因此，不安全或不相关的记忆一旦被注入上下文，比普通的提示注入更难防御。论文旨在将记忆搜索本身视为一个**信任边界**，并提出新的机制来确保只有上下文恰当的记忆才能影响当前任务。
*   **整体含义**：这项工作挑战了“相似即可用”的传统前提，主张个人AI的个性化必须从“无条件的记忆检索”转向“任务条件的记忆准入”。

### **2. 论文提出的方法论：核心思想、关键技术细节**

*   **核心思想**：论文提出 **MemGate**，一个轻量级的、可部署的“记忆搜索插件”。其核心思想是用一个**查询条件化的神经门控网络**来评估和筛选候选记忆，将标准的余弦相似度搜索转变为**任务条件记忆准入**。
*   **关键技术细节**：
    1.  **架构位置**：MemGate 部署在**向量数据库和骨干LLM之间**，无需修改LLM、重写数据库或引入推理时LLM评判者。
    2.  **输入特征**：输入是查询嵌入（\( q \)）和候选记忆嵌入（\( v_m \)）的交互向量：\( x_{inter} = [q \oplus v_m \oplus (q \odot v_m)] \in \mathbb{R}^{3d} \)。
    3.  **门控网络**：一个多层感知机（MLP）处理 \( x_{inter} \)，输出一个动态门控掩码 \( G_\phi(q, v_m) \in [0, 1]^d \)。该掩码对记忆嵌入的每个维度进行选择性衰减。
    4.  **门控相似度计算**： \( S_{MemGate}(q, v_m) = \frac{q \cdot (v_m \odot G_\phi(q, v_m))}{\|q\|_2 \|v_m \odot G_\phi(q, v_m)\|_2} \)。这个新的相似度分数替代原始余弦相似度进行重排序。
    5.  **训练目标**：采用基于**直接偏好优化（DPO）** 的偏好对齐训练。训练数据是三元组 \( (q, m^+, m^-) \)，分别代表查询、适配记忆和不适配记忆。目标是最小化DPO损失，同时加入一个**正记忆保持惩罚项** \( \lambda \cdot \mathbb{E}[\frac{1}{d}\|\mathbb{1}_d - g_+\|_1] \)，确保对合法、有益的记忆的表示不会因门控而过大偏离。
    6.  **理论分析**：论文提供了定理1和2，证明正记忆保持惩罚能约束有用记忆的语义漂移，同时能确保对不安全记忆的抑制。

### **3. 实验设计：数据集、Benchmark、对比方法**

*   **评估场景与Benchmark**：
    *   **可信度评估**：
        *   **良性过度个性化**：使用 **PersistBench**（评估跨域泄露和谄媚）和 **MemDrift**（评估工具调用漂移）。
        *   **恶意记忆操纵**：使用 **PS-Bench**（评估记忆诱导的越狱攻击）。
    *   **效用评估**：
        *   通用长时记忆能力：**LoCoMo** 和 **MemoryAgentBench**。
        *   个性化效用：**PreFEval**、**PersonaMem** 以及 PersistBench 中的有益记忆子集。
*   **对比方法**：
    *   **记忆框架**：评估了 **A-Mem**、**Mem0**、**MemOS** 以及一个真实世界个人代理系统 **OpenClaw**。
    *   **无记忆基线**：使用无记忆版本（`w/o Mem`）隔离长期记忆带来的影响。
    *   **多种LLM骨干**：在 **Qwen-3-8B**、**Llama-3.3-70B**、**GPT-4o-mini**、**GPT-5.1**、**Gemini-3-Pro** 和 **Claude-Sonnet-4.6** 六种模型上进行了验证。
*   **评估指标**：
    *   可信度：失败率（FR）、攻击成功率（ASR）。
    *   效用：F1分数、准确率、有益失败率（越低越好）。

### **4. 资源与算力**

*   **文中明确说明的内容**：
    *   **模型规模**：MemGate 仅 **9M 参数**，模型文件大小为 **35.1MB**。
    *   **训练配置**：使用 **AdamW** 优化器，学习率 **1.5×10⁻⁴**，batch size **256**，训练 **20 个epoch**。
    *   **训练数据**：在 **1,640** 个合成偏好对上进行训练。
*   **文中未明确说明的内容**：
    *   论文**没有明确提及**训练 MemGate 所使用的具体 GPU 型号、数量以及训练耗时。这部分信息缺失。

### **5. 实验数量与充分性**

*   **实验数量**：论文进行了**大量**且**系统**的实验：
    *   在**4种不同记忆框架**和**1种真实代理**上进行评估。
    *   涵盖了**6种不同规模和品牌的LLM**。
    *   在**4种主要威胁类型**（跨域泄露、谄媚、工具漂移、越狱）上进行了测试。
    *   在**5个基准**（LoCoMo, MemoryAgentBench, PreFEval, PersonaMem, PersistBench Beneficial）上评估了效用。
    *   对 **超参数**（正则化权重 \( \lambda \)、检索数量 top-k）和 **编码器** 进行了充分的消融研究。
    *   进行了**可视化分析**（t-SNE）展示嵌入空间变化。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖面广泛，考虑了不同代理框架、LLM、威胁模型和效用维度，设计周密。
    *   **客观性**：实验设计较为客观，比较了无记忆、有记忆和有MemGate的场景。数据均来自第三方基准。
    *   **公平性**：MemGate 无需修改LLM或数据库，与其他方法相比是一个更轻量、更公平的改进方案。论文也讨论了其局限性（如对谄媚的缓解效果不如其他威胁）。

### **6. 论文的主要结论与发现**

1.  **语义相似性搜索是安全缺陷**：基于语义相似性的记忆检索会引入跨域泄漏、谄媚、工具漂移和越狱等严重安全威胁，形成了“个性化-安全”的权衡。
2.  **MemGate 有效提升可信度**：在 GPT-4o-mini 上，MemGate 可将跨域泄漏从 27.0% 降至 3.5%，越狱攻击成功率从 16.8% 降至 4.4%。在 **Gemini-3-Pro** 和 **Claude-Sonnet-4.6** 等前沿模型上也表现显著。
3.  **不牺牲效用**：MemGate 在提升安全性的同时，不仅能保持长期记忆的效用，甚至在某些基准上（如 LoCoMo 从 38.9 到 40.8）还能**改善**整体得分。这表明它能够精准过滤有害或不相关的记忆，而不会误伤有益的个性化信息。
4.  **轻量且易部署**：MemGate 只有 9M 参数，引入的延迟极小（例如，在 OpenClaw 上从 1.47s 仅增加到 1.59s），可以无缝集成到现有记忆管道中，无需对 LLM 或数据库进行修改。
5.  **任务条件化的记忆准入是方向**：论文的核心结论是，可信的个性化需要从“无条件检索”转向**任务条件化的内存准入**，这是未来研究的可行方向。

### **7. 优点**

*   **方法创新性强**：将记忆搜索重构为“信任边界”，并提出“任务条件化的记忆准入”概念，理论贡献扎实。
*   **实用性与可部署性极佳**：MemGate 轻量、无需改动已有基础设施，有很大的实际应用价值。
*   **实验设计全面且严格**：覆盖了多种主流框架、多种LLM、多种威胁类型和效用评估，并且进行了充分的消融和理论分析，结果具有说服力。
*   **理论与实践的深度结合**：不仅有扎实的实验验证，还提供了数学定理（定理1和2）来支撑其方法的理论依据。
*   **对社区贡献明确**：明确指出并演示了记忆系统带来的四种具体威胁，为后续研究提供了清晰的框架。

### **8. 不足与局限**

*   **潜在的训练数据偏差**：MemGate 的训练数据（偏好对）由 GPT-4o-mini 合成。可能存在一定的模型偏差，可能会影响其在完全不同的应用场景上的泛化能力。
*   **谄媚问题缓解不彻底**：论文指出，虽然 MemGate 能有效降低谄媚，但效果不如对其他威胁（如跨域泄漏）显著，原因在于谄媚可能还与 LLM 自身的固有倾向有关。
*   **跨域泛化能力未充分证明**：论文主要评估了已有的几个基准，对于代理面临**新类型**、**非结构化的** 上下文不适配情形，MemGate 的表现如何尚未验证。
*   **缺乏算力开销的细节**：虽然轻量，但未提供详细的 GPU 训练耗时、GPU 型号等信息，这不利于研究者复现其训练过程。
*   **全依赖语义门控**：MemGate 完全在语义嵌入空间内操作，对于需要**理解任务逻辑**或**执行复杂推理**才能判断记忆是否不适配的情况，其有效性可能受限。

（完）
